# CineBook Backend (MySQL)

## Setup
1. Copy `.env.example` to `.env` and fill in your MySQL password
2. Run `npm install`
3. Create tables in MySQL Workbench
4. Run `npm run seed`
5. Run `npm run dev`
